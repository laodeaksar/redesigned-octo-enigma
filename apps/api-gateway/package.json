{
  "name": "api-gateway",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "description": "API Gateway — single entry point for all clients (Hono.js)",
  "license": "MIT",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist node_modules"
  },
  "dependencies": {
    "@bull-board/api": "^5.19.0",
    "@bull-board/hono": "^5.19.0",
    "@hono/swagger-ui": "^0.4.1",
    "@hono/zod-validator": "^0.2.2",
    "@repo/common": "workspace:*",
    "@repo/database": "workspace:*",
    "@repo/env": "workspace:*",
    "drizzle-orm": "^0.41.0",
    "hono": "^4.4.0",
    "ioredis": "^5.4.1",
    "jose": "^5.3.0",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
